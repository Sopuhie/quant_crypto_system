================================================================================
CURSOR IMPLEMENTATION GUIDE: STRATEGY SMOKE TESTING AND SIGNAL FORCE TRIGGER
Target Environment: macOS (Single-User Lightweight Edition)
Core Task:
Temporary modify the MaTrendStrategy logic in strategies/ma_trend_strategy.py to loosen the entry thresholds. This creates a "Mock Test Strategy" that generates order trades immediately on the next close bar, allowing us to end-to-end verify if risk controller validation, ccxt execution, and web rendering work flawlessly.

MODIFIED FILE: LOOSENING LOGIC IN STRATEGIES/MA_TREND_STRATEGY.PY FOR MOCK TESTING
Path: strategies/ma_trend_strategy.py
Action Required:
Open strategies/ma_trend_strategy.py and temporarily modify the signal generation block inside the on_kline_update method. We will force it to trigger a buy order signal on any incoming closed bar if there is no open position, bypassing the strict golden cross formula just for this runtime test.

Locate the "Evaluation Strategy: Entry Execution" block:
Replace it with the following testing block to enforce a swift trade generation.

Modified Code Section for strategies/ma_trend_strategy.py:

    # Evaluation Strategy: Entry Execution (TEMPORARY TESTING OVERRIDE)
    if not self.has_position:
        # Force triggering a mock signal to test if the database and gateway pipelines are active
        logger.info("🧪 [TEST MODE] Forcing a Mock Buy Signal to test pipeline connectivity.")
        signal = self.build_signal(
            action="create",
            market_type=self.market_type,
            side="buy",
            order_type="market",
            quantity=self.order_quantity,
            client_order_id=client_id,
        )
        signals.append(signal)
CURSOR EXECUTION SHORTCUT PROMPT
Please open strategies/ma_trend_strategy.py, navigate to the on_kline_update method, and temporarily bypass the macd_golden_cross conditional check. Force it to always append a market buy signal if self.has_position is False so we can observe order processing behavior during sandbox verification.